// METADATA

export const PARAM_TYPES = "design:paramtypes";
export const INJECTABLE_METADATA = "instance:injectable";
export const COMMAND_OPTIONS_METADATA = "command:options";
export const MODULE_OPTIONS_METADATA = "module:options";

// STORE METADATA

export const STORE_METADATA = Symbol("store:metadata");
export const STATE_METADATA = Symbol("state:metadata");
export const DISPATCH_METADATA = Symbol("dispatch:metadata");

// ERROR MESSAGES

export const ERROR_MESSAGES = {
	DUPLICATED_INJECTABLE_DECORATOR: "",
};
